document.addEventListener('DOMContentLoaded', () => {
    console.log('Global Structural BIM Leaders website loaded successfully!');
    // Optional: Add interactive features here if needed in the future
});